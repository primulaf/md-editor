# v1.5.0 实施计划

日期：2026-07-20
依据：`docs/specs/2026-07-20-reading-editing-save-ux-1.5.0.md`
状态：已实施并完成回归验证

## 实施步骤

1. 建立阅读/编辑、来源和修改状态模型，改用标签页级文档持久化。
2. 调整工具栏和面板布局，实现阅读模式全宽预览与编辑模式切换。
3. 使用 File System Access API 实现打开、保存和另存为，并保留下载降级。
4. 增加首次写回授权说明、外部修改冲突检查和准确的状态反馈。
5. 更新快捷键、版本号、README 和版本说明。
6. 运行语法检查，并在桌面与窄屏页面验证核心工作流。

## 验证结果

- 新建、工具内打开、拖拽和双击接管文件均按来源进入正确状态。
- 阅读与编辑模式切换、分栏恢复以及多标签页状态隔离通过。
- 保存、另存为、HTML 导出、首次源文件授权和下载降级流程通过。
- 空文件、未保存提醒、外部修改检测和标签页文件名同步通过。
- 目录跳转、滚动高亮、折叠跟随和长标题单行省略通过。
- `npm test`、Manifest 解析和项目结构校验通过。
