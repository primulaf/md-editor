# v1.6.0 Mermaid 图表实施计划

日期：2026-07-23
依据：`docs/specs/2026-07-23-mermaid-rendering-1.6.0.md`
状态：已实施，等待试用反馈

## 实施步骤

1. 固定 Mermaid 版本并建立可重复执行的浏览器依赖同步脚本。
2. 将完整 Mermaid ESM 入口和按类型分块本地化到扩展目录。
3. 扩展 markdown-it 围栏规则，生成 Mermaid 占位节点和安全源码回退。
4. 实现延迟加载、修订号防护、串行渲染、LRU 缓存和逐块错误隔离。
5. 增加严格安全配置、输入规模限制和 SVG 二次净化。
6. 增加响应式、宽图滚动、加载与错误状态样式。
7. 让字号切换、预览恢复和 HTML 导出接入 Mermaid 渲染生命周期。
8. 增加完整入口校验、类型回归夹具并执行桌面和窄屏浏览器测试。
9. 更新版本号、README、项目结构和技术栈说明。

## 当前验证结果

- Mermaid 11.16.0 ESM 入口及 103 个运行分块已本地化，未包含 source map。
- 思维导图、流程图、时序图、类图、状态图、ER 图、甘特图、饼图、Journey、Git Graph、Timeline、Quadrant、XY Chart、Sankey、Block 和 Architecture 共 16 类图表通过。
- 未知类型正确回退源码；快速编辑未出现旧图覆盖新图。
- Mermaid 字体在 21px 与 13px 字号间正确重渲染。
- 800x900 窄屏下宽图在容器内部滚动，页面宽度保持 800px。
- HTML 导出状态成功，生成图表不依赖远程运行库。
- 恶意标签、事件属性、脚本链接和 frontmatter 安全覆盖测试通过。
- 远程图片在 Mermaid 执行前被拒绝，SVG 外部资源引用和远程 CSS 被清除。
- `npm test` 通过，`npm audit` 为 0 个漏洞。
